commit ebff5ba9e81e71fcb01c497b534692af6f036ac4
Author: Munjani <94053823+mxnjani@users.noreply.github.com>
Date:   Wed Mar 4 08:50:24 2026 +0700

    fix: refine undo/redo history tracking and isDirty evaluation

diff --git a/src/hooks/useHistory.ts b/src/hooks/useHistory.ts
index 185d81c..8c099dc 100644
--- a/src/hooks/useHistory.ts
+++ b/src/hooks/useHistory.ts
@@ -71,7 +71,7 @@ export function useHistory({ onRestore }: UseHistoryOptions) {
             clearTimeout(debounceTimerRef.current);
             debounceTimerRef.current = null;
         }
-        historyRef.current = [{ value, cursor: 0 }];
+        historyRef.current = [{ value, cursor: value.trimEnd().length }];
         historyIndexRef.current = 0;
         nextCursorRef.current = null;
     }, []);
diff --git a/src/hooks/useMarkdownEditor.ts b/src/hooks/useMarkdownEditor.ts
index 607caa2..6644eec 100644
--- a/src/hooks/useMarkdownEditor.ts
+++ b/src/hooks/useMarkdownEditor.ts
@@ -33,9 +33,10 @@ console.log("Hello mdED!");
 
 export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | null>) {
     const [markdown, setMarkdown] = useState(DEFAULT_MARKDOWN);
+    const [originalMarkdown, setOriginalMarkdown] = useState(DEFAULT_MARKDOWN);
     const [fileName, setFileName] = useState('untitled.md');
     const [filePath, setFilePath] = useState<string | null>(null);
-    const [isDirty, setIsDirty] = useState(false);
+    const isDirty = markdown !== originalMarkdown;
     const [viewMode, setViewMode] = useState<ViewMode>('editor');
     const [isFullscreen, setIsFullscreen] = useState(false);
     const [showCloseConfirm, setShowCloseConfirm] = useState(false);
@@ -84,7 +85,6 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
     const { push: pushToHistory, undo, redo, reset: resetHistory, nextCursorRef, lastValue } = useHistory({
         onRestore: (md) => {
             setMarkdown(md);
-            setIsDirty(true);
         },
     });
 
@@ -94,6 +94,7 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
      */
     const applyFileContent = useCallback((content: string, path: string) => {
         setMarkdown(content);
+        setOriginalMarkdown(content);
         resetHistory(content);
         const name = path.split(/[/\\]/).pop();
         if (name) {
@@ -101,7 +102,6 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
             addRecentFile(path, name);
         }
         setFilePath(path);
-        setIsDirty(false);
     }, [resetHistory, addRecentFile]);
 
     const handleImagePaste = useCallback(async (file: File) => {
@@ -141,7 +141,6 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
             const newCursor = start + imageMarkdown.length;
 
             setMarkdown(newText);
-            setIsDirty(true);
             pushToHistory(newText, newCursor, true);
             nextCursorRef.current = newCursor;
         } catch (err) {
@@ -167,16 +166,23 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
             }
 
             const saved = localStorage.getItem('mded_content');
+            const savedOriginal = localStorage.getItem('mded_original_content');
             const savedName = localStorage.getItem('mded_filename');
             const savedMode = localStorage.getItem('mded_viewmode') as ViewMode;
             const savedDirty = localStorage.getItem('mded_isdirty') === 'true';
+
+            if (savedOriginal !== null) {
+                setOriginalMarkdown(savedOriginal);
+            } else if (saved && !savedDirty) {
+                setOriginalMarkdown(saved);
+            }
+
             if (saved) {
                 setMarkdown(saved);
                 resetHistory(saved);
             }
             if (savedName) setFileName(savedName);
             if (savedMode) setViewMode(savedMode);
-            setIsDirty(savedDirty);
         };
 
         init();
@@ -185,6 +191,7 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
     // Persist state to localStorage and sync dark mode class
     useEffect(() => {
         localStorage.setItem('mded_content', markdown);
+        localStorage.setItem('mded_original_content', originalMarkdown);
         localStorage.setItem('mded_filename', fileName);
         localStorage.setItem('mded_viewmode', viewMode);
         localStorage.setItem('mded_isdirty', String(isDirty));
@@ -195,7 +202,7 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
         } else {
             document.documentElement.classList.remove('dark');
         }
-    }, [markdown, fileName, viewMode, isDarkMode, isDirty]);
+    }, [markdown, originalMarkdown, fileName, viewMode, isDarkMode, isDirty]);
 
     /**
      * Insert text at the current cursor position in the editor.
@@ -211,7 +218,6 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
         const newText = text.substring(0, start) + before + selectedText + after + text.substring(end);
         const newCursor = start + before.length + selectedText.length;
         setMarkdown(newText);
-        setIsDirty(true);
         pushToHistory(newText, newCursor, true);
         nextCursorRef.current = start + before.length;
     }, [editorRef, pushToHistory, nextCursorRef]);
@@ -233,7 +239,8 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
                             addRecentFile(pathString, newFileName);
                         }
                         setFilePath(pathString);
-                        setIsDirty(false);
+                        setOriginalMarkdown(markdown);
+                        resetHistory(markdown);
                     } catch (writeErr) {
                         console.error("Failed to write to file natively:", writeErr, typeof writeErr === 'object' ? JSON.stringify(writeErr) : writeErr);
                         alert(`Save failed: ${writeErr}`);
@@ -257,7 +264,8 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
         if (isTauri() && filePath) {
             try {
                 await writeTextFile(filePath, markdown);
-                setIsDirty(false);
+                setOriginalMarkdown(markdown);
+                resetHistory(markdown);
             } catch (writeErr) {
                 console.error("Failed to quick save:", writeErr);
                 alert(`Save failed: ${writeErr}`);
@@ -273,19 +281,19 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
             setShowNewFileConfirm(true);
         } else {
             setMarkdown(DEFAULT_MARKDOWN);
+            setOriginalMarkdown(DEFAULT_MARKDOWN);
             resetHistory(DEFAULT_MARKDOWN);
             setFileName('untitled.md');
             setFilePath(null);
-            setIsDirty(false);
         }
     }, [isDirty, resetHistory]);
 
     const confirmNewFile = useCallback(() => {
         setMarkdown(DEFAULT_MARKDOWN);
+        setOriginalMarkdown(DEFAULT_MARKDOWN);
         resetHistory(DEFAULT_MARKDOWN);
         setFileName('untitled.md');
         setFilePath(null);
-        setIsDirty(false);
         setShowNewFileConfirm(false);
     }, [resetHistory]);
 
@@ -294,9 +302,9 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
         reader.onload = (event) => {
             const content = event.target?.result as string;
             setMarkdown(content);
+            setOriginalMarkdown(content);
             resetHistory(content);
             setFileName(file.name);
-            setIsDirty(false);
         };
         reader.readAsText(file);
     }, [resetHistory]);
@@ -310,7 +318,6 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
             loadFile(pendingOpenFile.file);
         }
 
-        setIsDirty(false);
         setShowOpenFileConfirm(false);
         setPendingOpenFile(null);
     }, [pendingOpenFile, loadFile, applyFileContent]);
@@ -486,7 +493,7 @@ export function useMarkdownEditor(editorRef: RefObject<HTMLTextAreaElement | nul
 
     return {
         markdown,
-        setMarkdown: (md: string) => { setMarkdown(md); setIsDirty(true); },
+        setMarkdown,
         fileName,
         filePath,
         isDirty,
