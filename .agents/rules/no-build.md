---
trigger: always_on
---

Do not execute build, development, installation, initialization, or update commands (e.g., npm dev, npm build, tauri dev, tauri build, npm install, npm init). Avoid any actions that perform long-running tasks or tasks outside the environment's reach. Provide a guide for the user to run them manually if needed.
